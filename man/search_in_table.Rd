% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comex_stat_tabelas.R
\name{search_in_table}
\alias{search_in_table}
\title{Buscar Termo em Tabela de Referência}
\usage{
search_in_table(codigo_tabela, termo_busca, coluna = NULL)
}
\arguments{
\item{codigo_tabela}{Caractere. O código da tabela de referência onde a busca será feita.
(Ex: "1" para NCM, "3" para Países).}

\item{termo_busca}{Caractere. O termo a ser buscado. A busca não diferencia maiúsculas de minúsculas.}

\item{coluna}{Caractere opcional. O nome da coluna específica onde a busca será realizada.
Se NULL, a busca será feita em todas as colunas de texto da tabela.
Padrão é NULL.}
}
\value{
Um data.frame (tibble) contendo os registros que correspondem ao termo de busca.
Retorna um data.frame vazio se nenhum registro for encontrado.
}
\description{
Realiza uma busca por um termo específico dentro de uma tabela de referência
do Comex Stat, permitindo filtrar por uma coluna específica ou em todas
as colunas de texto da tabela.
}
\examples{
\dontrun{
# Buscar "café" na tabela NCM
cafe_ncm <- search_in_table("1", "café")
print(head(cafe_ncm))

# Buscar países que contêm "Unidos" na coluna de nome do país
paises_unidos <- search_in_table("3", "Unidos", coluna = "NO_PAIS")
print(paises_unidos)
}
}
